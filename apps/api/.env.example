
# ============================================================================================
# IMPORTANT: DO NOT DELETE THIS FILE
# --------------------------------------------------------------------------------------------
# This `.env.example` file is required for the repository. It serves as a reference template
# showing the environment variables needed to run the project. Actual values must be provided
# via Infisical or your own `.env` file, but this file should remain in version control.
#
# For setup instructions and details on configuring environment variables, see:
# https://docs.heygaia.io/configuration/environment-variables
# ============================================================================================

# =============================
# Core application configuration
# =============================
ENV=development                      # Environment type: development, staging, production
HOST=http://localhost:8000           # Backend host URL
FRONTEND_URL=http://localhost:3000   # Frontend URL
DISABLE_PROFILING=true               # Disable profiling (optional)

# =============================
# Databases & Queues
# =============================
POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/langgraph  # pragma: allowlist secret
MONGO_DB=mongodb://mongo:27017/gaia
REDIS_URL=redis://redis:6379
CHROMADB_HOST=chromadb
CHROMADB_PORT=8000
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/  # pragma: allowlist secret

# =============================
# Auth (WorkOS mandatory, Google OAuth optional in dev)
# =============================
WORKOS_API_KEY=your-workos-api-key
WORKOS_CLIENT_ID=your-workos-client-id
WORKOS_COOKIE_PASSWORD=your-random-cookie-secret
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================
# Infisical Secrets Management (Only add if you're actually using Infisical for .env management)
# =============================
INFISICAL_PROJECT_ID=                # Project ID from Infisical dashboard
INFISICAL_MACHINE_IDENTITY_CLIENT_ID=      # Machine identity client ID for authentication
INFISICAL_MACHINE_IDENTITY_CLIENT_SECRET=  # Machine identity client secret for authentication

# =============================
# LangSmith for tracing (optional)
# =============================
LANGSMITH_TRACING=false              # Enable LangSmith tracing
LANGSMITH_API_KEY=                   # LangSmith API key

# =============================
# Bot Configuration
# =============================
GAIA_BOT_API_KEY=your-secure-internal-api-key

# Bot Session Token Configuration (JWT-based authentication)
# REQUIRED for production: Dedicated JWT secret (min 32 chars) - DO NOT reuse GAIA_BOT_API_KEY
# Generate with: openssl rand -hex 32
BOT_SESSION_TOKEN_SECRET=
# BOT_SESSION_TOKEN_EXPIRY_MINUTES=15                # Optional: Token expiry in minutes (default: 15)

# Platform Bot Tokens
DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=
SLACK_APP_TOKEN=
TELEGRAM_BOT_TOKEN=

# Bot OAuth Configuration (Optional - enables OAuth linking for better UX)
# If not set, users link accounts via bot commands (manual flow)
DISCORD_OAUTH_CLIENT_ID=
DISCORD_OAUTH_CLIENT_SECRET=
SLACK_OAUTH_CLIENT_ID=
SLACK_OAUTH_CLIENT_SECRET=
TELEGRAM_BOT_USERNAME=gaia_bot


# =============================
# Notes:
# - Only add the integrations you use. Missing keys will disable those features.
# - For production, see ProductionSettings in backend/app/config/settings.py for required keys.
# - Most API keys should be stored in Infisical or your own .env file, not committed to version control.
# - For voice agent configuration, see: https://docs.heygaia.io/configuration/voice-agent
# - For more details, see: https://docs.heygaia.io/configuration/environment-variables
